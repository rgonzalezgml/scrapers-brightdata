"""brightdata-scrapers middleware packages.

Each subpackage (e.g. ``cosmetics_design``) wraps one BrightData scraper as a
stateless Python client (see ``docs/specs/memory.md`` Etapa 3 and
``docs/fase3/README.md``).
"""
