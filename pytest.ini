[pytest]
asyncio_mode = auto
testpaths = middlewares tests
filterwarnings =
    ignore::DeprecationWarning
markers =
    brightdata: live BrightData API test (skipped unless BRIGHTDATA_API_KEY and per-scraper DATASET env vars are set)
